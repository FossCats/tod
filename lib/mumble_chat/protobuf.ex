defmodule MumbleChat.Protobuf do
  # This module is now just a placeholder
  # We're using MumbleChat.ProtobufHelper for manual protobuf handling

  # The original code has been commented out since we switched to manual handling
  # use Protobuf, from: Path.expand("priv/Mumble.proto"), namespace: :mumble
end
